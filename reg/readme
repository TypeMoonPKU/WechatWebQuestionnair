1. 每个家长只能注册一次，可能存在后续问题

2. 老师和家长注册时，依次检查name和openID是否为空。如果name为空，直接报错，不检测openID。

3. 每个学生只能注册一次（只属于一个班级）

4. 对csv文件进行学生注册检查，正确的加入数据库，不正确的不加入，返回成功添加的学生数、已注册的学生数、添加失败的学生数，但无法返回哪几个学生注册失败
   没有返回已注册学生的信息（可能已被其他老师注册，需要反馈？）
   不用csv了，改为由家长注册学生

5. 通知发布将确认作为一个option，前端是否会存在显示问题？  显示为普通选项形式好像有点丑